📝 Telegram + ChatGPT + Google Sheet Bot Setup

1. Replace "your-telegram-bot-token" in main.py with your Telegram BotFather token.
2. Replace "your-openai-api-key" with your OpenAI API key.
3. Place your Google service account JSON file as "credentials.json" in the same folder.
4. Make sure the Google Sheet is shared with the service account email.
5. Run: pip install -r requirements.txt
6. Run the bot: python main.py